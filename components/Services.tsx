"use client";
import { Check, Plus, Sparkles } from "lucide-react";
import { AnimatePresence, motion } from "framer-motion";
import { useState } from "react";
import { services } from "@/data/site";
import { Section } from "./Section";
export function Services(){const [active,setActive]=useState("software");const item=services.find(s=>s.id===active)!;return <Section id="services" eyebrow="CAPABILITIES" title="Technology that fits the problem, not the other way around."><div className="grid gap-4 lg:grid-cols-[1fr_1fr]"><div className="space-y-3">{services.map(s=><motion.button layout key={s.id} onClick={()=>setActive(s.id)} whileTap={{scale:.985}} className={`gradient-border relative w-full overflow-hidden rounded-3xl p-6 text-left transition ${active===s.id?"bg-[#151B26]":"bg-[#0F141D]/55 hover:bg-[#151B26]"}`}><div className="relative flex items-start justify-between gap-5"><div><div className="mb-5 text-xs font-extrabold tracking-[.2em]" style={{color:s.accent}}>{s.number}</div><h3 className="text-xl font-extrabold">{s.title}</h3><p className="mt-2 max-w-md text-sm leading-6 text-[#9AA4B2]">{s.short}</p></div><div className={`grid size-10 shrink-0 place-items-center rounded-full border transition ${active===s.id?"border-white/20 bg-white/10":"border-[#26303D]"}`}>{active===s.id?<Sparkles size={16}/>:<Plus size={16}/>}</div></div></motion.button>)}</div><div className="gradient-border relative min-h-[330px] overflow-hidden rounded-3xl bg-[#151B26] p-7 sm:p-9"><motion.div key={item.id} initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} transition={{duration:.45}}><div className="flex items-center gap-2 text-xs font-extrabold tracking-[.18em] text-[#9AA4B2]"><span className="size-2 rounded-full" style={{background:item.accent,boxShadow:`0 0 18px ${item.accent}`}}/> ACTIVE CAPABILITY</div><h3 className="mt-5 text-3xl font-extrabold tracking-tight">{item.title}</h3><p className="mt-3 max-w-lg leading-7 text-[#9AA4B2]">{item.short}</p><div className="mt-8 grid gap-3 sm:grid-cols-2">{item.bullets.map(b=><div key={b} className="flex gap-3 rounded-2xl border border-[#26303D] bg-black/10 p-4 text-sm text-[#DCE2E9]"><Check size={17} style={{color:item.accent}} className="mt-0.5 shrink-0"/>{b}</div>)}</div></motion.div><div className="pointer-events-none absolute -bottom-24 -right-12 size-52 rounded-full blur-3xl" style={{background:`${item.accent}18`}}/></div></div></Section>}
