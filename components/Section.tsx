import { Reveal } from "./Motion";
export function Section({id,eyebrow,title,children,muted=false}:{id:string;eyebrow?:string;title:string;children:React.ReactNode;muted?:boolean}){return <section id={id} className={`relative px-5 py-24 sm:px-8 lg:px-12 lg:py-32 ${muted?"bg-[#0F141D]/45":""}`}><div className="mx-auto max-w-7xl"><Reveal><div className="mb-12 max-w-3xl">{eyebrow&&<div className="mb-4 text-xs font-extrabold tracking-[.22em] text-[#19D3FF]">{eyebrow}</div>}<h2 className="text-4xl font-extrabold tracking-[-.04em] sm:text-5xl lg:text-6xl">{title}</h2></div></Reveal>{children}</div></section>}
