"use client";
import { Menu, X, ArrowUpRight } from "lucide-react";
import { AnimatePresence, motion } from "framer-motion";
import { useEffect, useState } from "react";
import { navItems } from "@/data/site";
export function Navbar(){const [active,setActive]=useState("Home"),[open,setOpen]=useState(false);useEffect(()=>{const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&setActive(e.target.id)),{rootMargin:"-35% 0px -55%"});navItems.forEach(n=>{const el=document.getElementById(n.toLowerCase().replace(" ","-"));if(el)obs.observe(el)});return()=>obs.disconnect()},[]);const go=(n:string)=>{setOpen(false);document.getElementById(n.toLowerCase().replace(" ","-"))?.scrollIntoView({behavior:"smooth"})};return <header className="glass fixed inset-x-0 top-0 z-50"><nav className="mx-auto flex h-[76px] max-w-7xl items-center justify-between px-5 sm:px-8 lg:px-12"><button onClick={()=>go("Home")} className="group flex items-center gap-3"><span className="relative grid size-9 place-items-center rounded-xl bg-white/[.06] ring-1 ring-white/10"><span className="absolute size-2 rounded-full bg-[#19D3FF] shadow-[0_0_20px_#19D3FF]"/><span className="size-5 rounded-lg border border-white/40 rotate-45 group-hover:rotate-90 transition-transform"/></span><span className="font-extrabold tracking-tight">Manellore <span className="text-[#9AA4B2]">Groups</span></span></button><div className="hidden items-center gap-1 md:flex">{navItems.map(n=><button key={n} onClick={()=>go(n)} className={`rounded-full px-3 py-2 text-xs font-bold transition ${active===n.toLowerCase().replace(" ","-")||active===n?"bg-white/[.08] text-white":"text-[#9AA4B2] hover:text-white"}`}>{n}</button>)}</div><button onClick={()=>setOpen(v=>!v)} className="grid size-11 place-items-center rounded-full border border-[#26303D] md:hidden">{open?<X size={19}/>:<Menu size={19}/>}</button><button onClick={()=>go("Contact")} className="hidden items-center gap-2 rounded-full bg-white px-4 py-2.5 text-xs font-extrabold text-[#080B12] md:flex">Start a conversation <ArrowUpRight size={15}/></button></nav><AnimatePresence>{open&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-[#26303D] bg-[#080B12]/95 md:hidden"><div className="p-4">{navItems.map(n=><button key={n} onClick={()=>go(n)} className="block w-full rounded-xl px-4 py-3 text-left font-bold text-[#9AA4B2] hover:bg-white/[.05] hover:text-white">{n}</button>)}</div></motion.div>}</AnimatePresence></header>}
